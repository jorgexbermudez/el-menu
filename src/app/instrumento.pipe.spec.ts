import { InstrumentoPipe } from './instrumento.pipe';

describe('InstrumentoPipe', () => {
  it('create an instance', () => {
    const pipe = new InstrumentoPipe();
    expect(pipe).toBeTruthy();
  });
});
