import { NuevalineaPipe } from './nuevalinea.pipe';

describe('NuevalineaPipe', () => {
  it('create an instance', () => {
    const pipe = new NuevalineaPipe();
    expect(pipe).toBeTruthy();
  });
});
