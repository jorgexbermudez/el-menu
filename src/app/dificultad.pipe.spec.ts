import { DificultadPipe } from './dificultad.pipe';

describe('DificultadPipe', () => {
  it('create an instance', () => {
    const pipe = new DificultadPipe();
    expect(pipe).toBeTruthy();
  });
});
